Rates refresh in 
<span class="timer text-fx-orange"></span>

<script type="module">
    let seconds = 59;
    setInterval(() => {
        document.querySelector(".timer").innerHTML = `${seconds}s`;
        if (seconds === 0) {
            seconds = 59;
        } else {
            --seconds;
        }
    }, 1000);
</script>